import Button from "@/components/atoms/button";
import LoginButton from "@/components/LoginButton";
import { useRouter } from "next/router";

const Home = () => {
  const router = useRouter();

  return <div className="overflow-x-hidden bg-zinc-900"></div>;
};
export default Home;
